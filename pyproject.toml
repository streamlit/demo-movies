[project]
name = "movies-demo"
version = "0.1.0"
description = "Streamlit demo exploring the Movies dataset."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.52",
    "polars>=1.35",
    "scikit-learn>=1.7.2",
    "statsmodels>=0.14.6",
]
